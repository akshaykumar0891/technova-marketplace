PORT=5000
NODE_ENV=development
JWT_SECRET=generate_a_long_random_secret_string

# Database Configuration: 'sqlite' or 'mysql'
DB_TYPE=sqlite

# MySQL Details (Only required if DB_TYPE=mysql)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_mysql_password
DB_NAME=technova_db
